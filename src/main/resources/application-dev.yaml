spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/modern_dentistry
    username: postgres
    password: "012345"
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379